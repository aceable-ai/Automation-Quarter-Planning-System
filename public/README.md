# Public assets for the reward feature demo

Drop these two files here before the demo:

- `demo_song.mp3` — Peggy's pre-generated Suno song (mock provider returns this URL)
- `demo_celebration.png` — Pre-cached celebration image used as fallback when OpenAI errors
   AND used for the demo master plan if `DEMO_MASTER_PLAN_ID` env var matches the plan being shipped
